// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.mol0tok.cryptoinfo"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
